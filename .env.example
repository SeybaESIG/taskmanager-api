# Copy this file to `.env` for local development.

# PostgreSQL settings used by docker-compose and Spring Boot
POSTGRES_DB=db_taskmanager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_strong_password

# App-side DB host/port (for local app outside Docker)
DB_HOST=localhost
DB_PORT=5432

